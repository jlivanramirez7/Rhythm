# Forcing a clean build to deploy GCS-backed SQLite migration.
steps:
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args: ['services', 'enable', 'secretmanager.googleapis.com']
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/rhythm-479516/rhythm', '.']
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/rhythm-479516/rhythm']
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'rhythm'
      - '--image'
      - 'gcr.io/rhythm-479516/rhythm'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--update-secrets=DB_USER=DB_USER:latest,DB_PASSWORD=DB_PASSWORD:latest,DB_NAME=DB_NAME:latest,GOOGLE_CLIENT_ID=GOOGLE_CLIENT_ID:latest,GOOGLE_CLIENT_SECRET=GOOGLE_CLIENT_SECRET:latest,SESSION_SECRET=SESSION_SECRET:latest'
      - '--set-env-vars=NODE_ENV=production,DB_ADAPTER=gcs-sqlite,GCS_BUCKET_NAME=rhythm-479516-db-bucket'
images:
  - 'gcr.io/rhythm-479516/rhythm'
options:
  logging: CLOUD_LOGGING_ONLY
